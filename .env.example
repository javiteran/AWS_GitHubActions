# Base de datos MySQL - CONFIGURAR ESTOS VALORES
DB_HOST=localhost
DB_USER=tu_usuario_mysql
DB_PASSWORD=tu_password_mysql
DB_NAME=AlumnosClases
DB_PORT=3306

# Configuración de Flask - CAMBIAR ESTOS VALORES
FLASK_DEBUG=False
FLASK_HOST=0.0.0.0
FLASK_PORT=80

# INSTRUCCIONES:
# 1. Copia este archivo como .env
# 2. Reemplaza todos los valores con tus credenciales reales
# 3. NUNCA subas el archivo .env al repositorio
# 4. El archivo .env ya está en .gitignore para tu protección